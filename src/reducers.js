import todosList from './todos.json'

import {
    TOGGLE_TODO,
    ADD_TODO,
    DELETE_TODO,
    CLEAR_COMPLETED_TODOS
} from './actions'

const initialState = {
    todos: todosList
}

const todosReducer = (state = initialState, action) {
    switch (action.type) {
        case
    }
}